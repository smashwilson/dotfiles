#!/bin/bash
#
# Toggle the active ssh config to be "config.offvpn".

if [[ ! -f ${HOME}/.ssh/config.offvpn ]]; then
  echo "No unVPN-specific configuration found."
  exit 1
fi

ln -f -s ${HOME}/.ssh/config.offvpn ${HOME}/.ssh/config
echo "VPN-less ssh configuration active."
