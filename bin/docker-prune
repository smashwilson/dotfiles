#!/bin/bash
#
# Prune dangling docker images.

set -euo pipefail
IFS=$'\n\r'

SUDO='sudo'
which boot2docker >/dev/null 2>&1 && SUDO=''

echo "Cleaning all dangling images."

IMAGES=$(${SUDO} docker images -f 'dangling=true' -q)
[ -n "${IMAGES}" ] && ${SUDO} docker rmi ${IMAGES}
